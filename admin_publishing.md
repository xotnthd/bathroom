# 관리자 페이지 퍼블리싱 가이드 (Admin Publishing Guide)

## 1. 기본 UI/UX 통일 기준 (Standard UI/UX)
- 관리자 화면의 모든 페이지는 **팝업 관리(`/admin/popup`) 화면의 팝업 모듈 형태 및 리스트 디자인**을 **최우선 기준(Standard)**으로 삼는다.
- 새로운 페이지를 만들거나 기존 페이지를 수정할 때 이 기준을 절대적으로 따라야 한다.

## 2. 세부 기준 가이드라인
1. **리스트와 모달 분리 구조**: 모든 등록/수정 동작은 별도의 컴포넌트로 분리된 모달창 안에서 이루어져야 한다.
2. **명확한 폼(Form) 레이아웃**: `.admin-form-row`, `.admin-form-label`, `.admin-form-control` 등 공통 CSS 클래스를 사용하여, 좌측은 연한 회색 배경의 라벨, 우측은 입력값이 있는 명확한 **테이블 형태의 모달 폼**을 구성한다.
3. **상단 검색 조건 규격화**: 상단에 검색 영역을 배치하되, 시스템 구분 등 공통 검색 조건은 라디오 버튼 등으로 직관적으로 제공하고 변경 시 즉각 반영(Auto-Trigger)되거나 조회 버튼을 거치도록 설계한다.
4. **수정 불가 항목 제어 로직**: 
   - 등록 후 수정할 수 없는 항목(예: 시스템 구분, 배포 시작일이 지난 데이터 등)은 상황에 맞춰 `disabled` 또는 `readonly` 처리를 필수적으로 적용한다.
   - 상태(시작일 도래 여부 등)에 따른 분기 처리를 꼼꼼하게 적용해 데이터 정합성을 해치지 않게 한다.
5. **명확한 버튼 디자인 (버튼 텍스트 래핑 방지)**:
   - 닫기 버튼은 연한 핑크색 등을 활용하여 가시성을 높이고 직관적인 '닫기' 문구를 사용한다.
   - 모든 기능 버튼(등록, 수정, 삭제)은 `.admin-btn` 클래스를 사용하고, 컨테이너 영역이 좁아지더라도 글자가 줄바꿈되지 않도록 `flex-shrink: 0`, `white-space: nowrap` 처리를 기본으로 한다.
6. **불필요한 이모티콘 사용 금지**: 시스템 UI에서 이모티콘(🗂, 🏷️ 등)은 전문성을 저해할 수 있으므로 특별한 요청이 없는 한 사용을 금지한다.
