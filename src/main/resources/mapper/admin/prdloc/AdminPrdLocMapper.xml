<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.community.bathroom.admin.prdloc.mapper.AdminPrdLocMapper">

    <select id="selectLocationList" parameterType="String" resultType="java.util.Map">
        SELECT idx AS "idx", sys_id AS "sysId", loc_nm AS "locNm", use_yn AS "useYn"
        FROM TN_PRD_LOC_M001 WHERE sys_id = #{sysId} AND del_yn = 'N' ORDER BY idx ASC
    </select>

    <select id="selectLocationByIdx" parameterType="java.util.Map" resultType="java.util.Map">
        SELECT idx AS "idx", sys_id AS "sysId", loc_nm AS "locNm", use_yn AS "useYn"
        FROM TN_PRD_LOC_M001 WHERE sys_id = #{sysId} AND idx = #{idx} AND del_yn = 'N'
    </select>

    <insert id="insertLocation" parameterType="java.util.Map" useGeneratedKeys="true" keyProperty="idx">
        INSERT INTO TN_PRD_LOC_M001 (sys_id, loc_nm, use_yn, del_yn, frst_rgstr_id, frst_reg_dt)
        VALUES (#{sysId}, #{locNm}, #{useYn}, 'N', #{userId}, NOW())
    </insert>

    <update id="updateLocation" parameterType="java.util.Map">
        UPDATE TN_PRD_LOC_M001 SET loc_nm = #{locNm}, use_yn = #{useYn}, last_mdfr_id = #{userId}, last_mdfcn_dt = NOW()
        WHERE idx = #{idx} AND sys_id = #{sysId}
    </update>

    <update id="logicalDeleteLocation">
        UPDATE TN_PRD_LOC_M001 SET del_yn = 'Y' WHERE sys_id = #{sysId} AND idx = #{idx}
    </update>

    <insert id="insertDefaultLocation">
        INSERT INTO TN_PRD_LOC_M001 (sys_id, loc_nm, use_yn, del_yn, frst_rgstr_id, frst_reg_dt)
        VALUES (#{sysId}, '본사', 'Y', 'N', 'admin', NOW())
    </insert>

</mapper>
